{
  "name": "sprite-mobile",
  "version": "1.0.0",
  "description": "Mobile-friendly web interface for chatting with Claude Code on a Sprite",
  "type": "module",
  "scripts": {
    "start": "bun --hot run server.ts",
    "dev": "bun --watch run server.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.966.0"
  }
}
